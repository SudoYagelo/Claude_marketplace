---
description: Автоматически исправить код 1С согласно стандартам разработки
---

Автоматически исправь указанный модуль 1С согласно стандартам разработки из документа `1C_Coding_Standards_LLM.md`.

## Инструкции для выполнения исправления

1. **Сначала выполни проверку**
   - Используй ту же логику, что и команда `/check-1c`
   - Определи все нарушения, которые можно исправить автоматически

2. **Определи безопасные для автоисправления нарушения:**

   ### ✅ Можно исправлять автоматически:
   - Форматирование кода (табуляция, пробелы)
   - Длинные строки (переносы)
   - Буква "ё" в идентификаторах
   - Закомментированный код (удаление)
   - Отладочный код (удаление TODO, MRG)
   - Структура областей (#Область / #КонецОбласти)
   - Добавление недостающих областей
   - Простые случаи CamelCase (если очевидно)
   - Добавление базовой документации для функций

   ### ⚠️ Требует подтверждения:
   - Переименование функций и процедур
   - Изменение порядка параметров
   - Изменение логики обработки "Отказ"
   - Добавление информирования пользователя

   ### ❌ Нельзя исправлять автоматически:
   - Сложные случаи именования (требуют понимания контекста)
   - Логические ошибки
   - Изменение функционала

3. **Для исправлений, требующих подтверждения:**
   - Спроси пользователя перед внесением изменений
   - Покажи что было и что будет
   - Дождись подтверждения

4. **Выполни исправления:**
   - Используй инструмент Edit для внесения изменений
   - Исправляй по категориям (сначала форматирование, потом структура, и т.д.)
   - Сохраняй функциональность кода

5. **Примеры исправлений:**

   ### Форматирование
   ```1c
   // БЫЛО:
   Функция ПолучитьДанные(  Параметр1,Параметр2  )
     Результат=Новый Массив;
      Возврат Результат;
   КонецФункции

   // СТАЛО:
   Функция ПолучитьДанные(Параметр1, Параметр2)
   	Результат = Новый Массив;
   	Возврат Результат;
   КонецФункции
   ```

   ### Буква "ё"
   ```1c
   // БЫЛО:
   Процедура ОбработатьДанныеСЁрвера()

   // СТАЛО:
   Процедура ОбработатьДанныеСервера()
   ```

   ### Удаление закомментированного кода
   ```1c
   // БЫЛО:
   Процедура Обработать()
   	// Устаревший код
   	// Если Истина Тогда
   	// 	Сообщить("Отладка");
   	// КонецЕсли;

   	ВыполнитьОбработку();
   КонецПроцедуры

   // СТАЛО:
   Процедура Обработать()
   	ВыполнитьОбработку();
   КонецПроцедуры
   ```

   ### Структура областей
   ```1c
   // БЫЛО:
   Функция ПолучитьДанные() Экспорт
   	// код
   КонецФункции

   Функция ВнутренняяФункция()
   	// код
   КонецФункции

   // СТАЛО:
   #Область ПрограммныйИнтерфейс

   Функция ПолучитьДанные() Экспорт
   	// код
   КонецФункции

   #КонецОбласти

   #Область СлужебныеПроцедурыИФункции

   Функция ВнутренняяФункция()
   	// код
   КонецФункции

   #КонецОбласти
   ```

   ### Добавление документации
   ```1c
   // БЫЛО:
   Функция ПолучитьКурсВалюты(Валюта, Дата) Экспорт
   	// код
   КонецФункции

   // СТАЛО:
   // Возвращает курс валюты на указанную дату.
   //
   // Параметры:
   //  Валюта - СправочникСсылка.Валюты - валюта для получения курса.
   //  Дата - Дата - дата, на которую требуется курс.
   //
   // Возвращаемое значение:
   //  Число - курс валюты на указанную дату.
   //
   Функция ПолучитьКурсВалюты(Валюта, Дата) Экспорт
   	// код
   КонецФункции
   ```

6. **Сформируй отчет об исправлениях**

   После внесения изменений выведи:
   - Количество исправленных нарушений
   - Список внесенных изменений по категориям
   - Нарушения, которые не были исправлены автоматически

## Формат отчета

```
# Отчет исправлений модуля: [Имя файла]

## ✅ Выполненные исправления

### 1. Форматирование (N исправлений)
- Исправлена табуляция в строках: 5, 10, 15-20
- Исправлены пробелы вокруг операторов: 25, 30
- Перенесены длинные строки: 42, 55

### 2. Структура модуля (N исправлений)
- Добавлена область #ПрограммныйИнтерфейс
- Добавлена область #СлужебныеПроцедурыИФункции
- Перемещены функции в соответствующие области

### 3. Чистота кода (N исправлений)
- Удален закомментированный код: строки 15-20, 45-50
- Удалены отладочные метки TODO: строки 30, 60

### 4. Именование (N исправлений)
- Исправлена буква "ё": ПолучитьДанныеСЁрвера → ПолучитьДанныеСервера

### 5. Документирование (N исправлений)
- Добавлена документация для функции ПолучитьКурсВалюты (строка 100)
- Добавлена документация для функции РассчитатьСумму (строка 150)

## ⚠️ Требуется ручное исправление

❌ Строка 75: Имя функции "ПолучитьМассивДанных" - рекомендуется "ДанныеМассива"
   Требуется подтверждение для переименования

❌ Строка 120: Порядок параметров не соответствует стандарту
   Рекомендуется изменить порядок: (Форма, ИмяПоля, Цвет)

## 📊 Статистика

- Всего исправлено автоматически: N
- Требует ручного исправления: N
- Уровень соответствия после исправлений: N%

## 💡 Рекомендации

1. Проверь исправленный код на корректность работы
2. Исправь оставшиеся нарушения вручную
3. Запусти тесты для проверки функциональности
```

## Важно

- **ВСЕГДА** делай резервную копию перед массовыми изменениями
- **НЕ ИСПРАВЛЯЙ** логику работы кода без подтверждения
- **СПРАШИВАЙ** пользователя при сомнениях
- **СОХРАНЯЙ** функциональность кода
- **ТЕСТИРУЙ** после исправлений (если возможно)
- **БУДЬ ОСТОРОЖЕН** с переименованием - это может сломать ссылки в других модулях

## Порядок исправлений

1. Форматирование (самое безопасное)
2. Удаление закомментированного/отладочного кода
3. Структура областей
4. Простые исправления именования (буква "ё")
5. Добавление документации
6. Сложные исправления (с подтверждением)
