---
description: Форматировать код 1С (отступы, пробелы, переносы строк)
---

Отформатируй указанный модуль 1С согласно правилам форматирования из стандартов разработки.

## Инструкции для форматирования

Эта команда выполняет **только форматирование кода** без изменения логики, именования и структуры.

### 1. Правила табуляции

- **Заменить пробелы на табуляцию** (размер табуляции = 4 символа)
- С крайней левой позиции (без отступа) начинаются только:
  - `Процедура`, `КонецПроцедуры`, `Функция`, `КонецФункции`
  - Директивы компилятора (`&НаКлиенте`, `&НаСервере`)
  - Аннотации (`&Перед`, `&После`)
  - Инструкции препроцессора (`#Область`, `#КонецОбласти`)
  - Объявления переменных модуля (`Перем`)
  - Комментарии к процедурам/функциям

```1c
// БЫЛО:
  Функция ПолучитьДанные()
      Результат = Новый Массив;
        Возврат Результат;
  КонецФункции

// СТАЛО:
Функция ПолучитьДанные()
	Результат = Новый Массив;
	Возврат Результат;
КонецФункции
```

### 2. Правила для транзакций

Транзакции НЕ являются операторными скобками (без дополнительного сдвига):

```1c
// БЫЛО:
НачатьТранзакцию();
	Попытка
		ЗаписатьДанные();
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
	КонецПопытки;

// СТАЛО:
НачатьТранзакцию();
Попытка
	ЗаписатьДанные();
	ЗафиксироватьТранзакцию();
Исключение
	ОтменитьТранзакцию();
КонецПопытки;
```

### 3. Пробелы вокруг операторов

- **Ставить пробелы**:
  - Вокруг операторов присваивания (`=`)
  - Вокруг операторов сравнения (`=`, `<>`, `>`, `<`, `>=`, `<=`)
  - Вокруг арифметических операторов (`+`, `-`, `*`, `/`, `%`)
  - Вокруг логических операторов (`И`, `ИЛИ`, `НЕ`)
  - После запятой в списках параметров

- **НЕ ставить пробелы**:
  - Перед открывающей скобкой функции
  - После открывающей скобки
  - Перед закрывающей скобкой
  - Перед запятой

```1c
// БЫЛО:
Результат=Функция1(Парам1,Парам2)+Функция2 ( Парам3 , Парам4 );
Если Значение1=Значение2И Флаг<>Истина Тогда

// СТАЛО:
Результат = Функция1(Парам1, Парам2) + Функция2(Парам3, Парам4);
Если Значение1 = Значение2 И Флаг <> Истина Тогда
```

### 4. Длина строк

Максимальная длина строки: **120 символов** (включая табуляцию = 4 символа)

**Исключение**: длинные строковые константы, которые выводятся без переносов

Способы переноса длинных строк:

#### 4.1. Перенос параметров функции
```1c
// БЫЛО:
Результат = ВыполнитьОченьДлиннуюФункциюСМножествомПараметров(Параметр1, Параметр2, Параметр3, Параметр4, Параметр5, Параметр6);

// СТАЛО:
Результат = ВыполнитьОченьДлиннуюФункциюСМножествомПараметров(
	Параметр1,
	Параметр2,
	Параметр3,
	Параметр4,
	Параметр5,
	Параметр6);
```

#### 4.2. Перенос строк запроса
```1c
// БЫЛО:
ТекстЗапроса = "ВЫБРАТЬ Контрагенты.Ссылка, Контрагенты.Наименование, Контрагенты.ИНН ИЗ Справочник.Контрагенты";

// СТАЛО:
ТекстЗапроса =
	"ВЫБРАТЬ
	|	Контрагенты.Ссылка,
	|	Контрагенты.Наименование,
	|	Контрагенты.ИНН
	|ИЗ
	|	Справочник.Контрагенты";
```

### 5. Один оператор в одной строке

```1c
// БЫЛО:
Индекс = 0; Результат = 0; Сумма = 0; ВыполнитьОбработку();

// СТАЛО (допустимо для присваиваний):
НачальныйИндекс = 0; Индекс = 0; Результат = 0;

// Но лучше:
НачальныйИндекс = 0;
Индекс = 0;
Результат = 0;
ВыполнитьОбработку();
```

### 6. Пустые строки

- **Одна пустая строка** между процедурами и функциями (если нет областей)
- **Не добавлять** лишние пустые строки внутри процедур
- **Допустимо** разделять логические блоки одной пустой строкой

```1c
// БЫЛО:
Функция Функция1()


	Результат = 1;


	Возврат Результат;
КонецФункции
Функция Функция2()
	Возврат 2;
КонецФункции

// СТАЛО:
Функция Функция1()
	Результат = 1;
	Возврат Результат;
КонецФункции

Функция Функция2()
	Возврат 2;
КонецФункции
```

## Процесс форматирования

1. **Прочитай файл**
   - Если пользователь не указал файл, попроси указать путь

2. **Примени правила форматирования в порядке:**
   - Табуляция вместо пробелов
   - Пробелы вокруг операторов
   - Пробелы в списках параметров
   - Перенос длинных строк
   - Удаление лишних пустых строк
   - Один оператор в строке

3. **Сохрани изменения**
   - Используй инструмент Edit для внесения изменений
   - Сохраняй весь остальной код без изменений

4. **Выведи отчет**

## Формат отчета

```
# Отчет форматирования модуля: [Имя файла]

## ✅ Выполненные изменения

- Исправлена табуляция: N строк
- Исправлены пробелы вокруг операторов: N строк
- Исправлены пробелы в параметрах: N строк
- Перенесены длинные строки: N строк
- Удалены лишние пустые строки: N
- Исправлены множественные операторы в строке: N строк

## 📊 Статистика

- Всего обработано строк: N
- Изменено строк: N
- Файл отформатирован успешно ✅
```

## Важно

- **НЕ ИЗМЕНЯЙ**: именование, логику, структуру областей, комментарии
- **ТОЛЬКО ФОРМАТИРОВАНИЕ**: отступы, пробелы, переносы
- **БЕЗОПАСНАЯ ОПЕРАЦИЯ**: не влияет на функциональность кода
- **СОХРАНЯЙ**: все комментарии и строковые константы как есть
- **ПРОВЕРЯЙ**: что не сломал многострочные строки

## Примеры комплексного форматирования

### До форматирования:
```1c
&НаСервере
Функция ПолучитьКурсВалюты(  Валюта,Дата  )
  Запрос=Новый Запрос;Запрос.Текст="ВЫБРАТЬ КурсыВалют.Курс ИЗ РегистрСведений.КурсыВалют КАК КурсыВалют ГДЕ КурсыВалют.Валюта=&Валюта И КурсыВалют.Период<=&Дата";
    Запрос.УстановитьПараметр("Валюта",Валюта);
      Запрос.УстановитьПараметр("Дата",КонецДня(Дата));Результат=Запрос.Выполнить();
  Если Результат.Пустой()Тогда Возврат 1;КонецЕсли;
Выборка=Результат.Выбрать();Выборка.Следующий();Возврат Выборка.Курс;
КонецФункции
```

### После форматирования:
```1c
&НаСервере
Функция ПолучитьКурсВалюты(Валюта, Дата)
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	КурсыВалют.Курс
		|ИЗ
		|	РегистрСведений.КурсыВалют КАК КурсыВалют
		|ГДЕ
		|	КурсыВалют.Валюта = &Валюта
		|	И КурсыВалют.Период <= &Дата";

	Запрос.УстановитьПараметр("Валюта", Валюта);
	Запрос.УстановитьПараметр("Дата", КонецДня(Дата));

	Результат = Запрос.Выполнить();

	Если Результат.Пустой() Тогда
		Возврат 1;
	КонецЕсли;

	Выборка = Результат.Выбрать();
	Выборка.Следующий();

	Возврат Выборка.Курс;
КонецФункции
```
